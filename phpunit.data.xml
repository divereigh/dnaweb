<?xml version="1.0" encoding="UTF-8"?>
<!--
  Tests that need real data, run against the local MariaDB snapshot.

  Separate from phpunit.xml because that one pins every suite to sqlite
  `:memory:`, which cannot hold this schema — it is owned by the Perl
  loaders, there are no migrations for it here, and the queries under
  test lean on MariaDB behaviour (FULLTEXT, the v_* views).

  DB_HOST and DB_PORT are pinned to the local snapshot rather than
  inherited from .env, so this suite reaches production even when .env
  is switched to the tunnel. DataTestCase refuses to run if it somehow
  lands anywhere else.
-->
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Data">
            <directory>tests/Data</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="DB_CONNECTION" value="mysql"/>
        <env name="DB_HOST" value="127.0.0.1" force="true"/>
        <env name="DB_PORT" value="3306" force="true"/>
        <env name="DB_URL" value=""/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
    </php>
</phpunit>
